{
  "name": "httptoolkit-mcp",
  "version": "1.0.0",
  "description": "MCP server to read live HTTP exchanges from HTTP Toolkit UI",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  }
}
